<div class="inter">
  <%= render 'layouts/header' %>
</div>

<section class="donate">
  <%= render 'donate_blood/donate_form' %>
</section>

<section class="what-happens">
  <%= render 'donate_blood/what_happens_with_blood' %>
</section>

<div class="inter">
  <%= render 'layouts/footer' %>
</div>
